services:
  db:
    image: postgres:15-alpine
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_USER: mihneas22
      POSTGRES_PASSWORD: 12345
      POSTGRES_DB: flashdesk_db
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      - app-network
  api:
    build:
      context: ./backend/FlashDeskAPI
      dockerfile: Dockerfile
    depends_on:
      - db
    env_file:
      - keys.env
    ports:
      - "5000:8080"
    networks:
      - app-network

  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "8080:3000"
    depends_on:
      - api
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  db-data: